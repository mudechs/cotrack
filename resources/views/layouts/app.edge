<!DOCTYPE html>
<html lang="{{ globals().default_locale }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, maximum-scale=1.0, user-scalable=no">
    <meta name="robots" content="noindex, nofollow, noarchive" />
    <title>{{ globals().app_name }}</title>

    <link rel="shortcut icon" href="{{ assetsUrl('favicons/favicon.ico') }}" type="image/x-icon" />
    <link rel="apple-touch-icon" href="{{ assetsUrl('favicons/apple-touch-icon.png') }}" />
    <link rel="apple-touch-icon" sizes="57x57" href="{{ assetsUrl('favicons/apple-touch-icon-57x57.png') }}" />
    <link rel="apple-touch-icon" sizes="72x72" href="{{ assetsUrl('favicons/apple-touch-icon-72x72.png') }}" />
    <link rel="apple-touch-icon" sizes="76x76" href="{{ assetsUrl('favicons/apple-touch-icon-76x76.png') }}" />
    <link rel="apple-touch-icon" sizes="114x114" href="{{ assetsUrl('favicons/apple-touch-icon-114x114.png') }}" />
    <link rel="apple-touch-icon" sizes="120x120" href="{{ assetsUrl('favicons/apple-touch-icon-120x120.png') }}" />
    <link rel="apple-touch-icon" sizes="144x144" href="{{ assetsUrl('favicons/apple-touch-icon-144x144.png') }}" />
    <link rel="apple-touch-icon" sizes="152x152" href="{{ assetsUrl('favicons/apple-touch-icon-152x152.png') }}" />

    <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
    {{ style('app') }}
  </head>
  <body>

    @include('partials/nav')

    @loggedIn
      @include('includes.toast')
    @endloggedIn

    <main role="main">
      <section class="section">
        @!section('content')
      </section>
    </main>

    @include('partials.footer')

    {{ script('app') }}

    @if(flashMessage('notification'))
      <script>
        $(document).ready(function () {
          $('.toast-wrapper').show();
          $('.toast').toast('show');
        });
      </script>
    @endif

    @!section('js')
  </body>
</html>